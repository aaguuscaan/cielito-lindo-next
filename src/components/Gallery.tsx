'use client';
import Image from 'next/image'; import { useState } from 'react';
const images = ['/images/cabins1.jpg','/images/cabins2.jpg','/images/cabins3.jpg','/images/cabins4.jpg','/images/cabins5.jpg','/images/cabin6.jpg'];
export function Gallery() { const [active, setActive] = useState<number | null>(null); const next = (delta: number) => setActive((value) => value === null ? 0 : (value + delta + images.length) % images.length); return <><section className="gallery-section" id="galeria"><div className="section-inner"><div className="fade-up"><span className="section-label">Galería</span><h2 className="section-title">Sentí el <em>ambiente</em></h2><p className="section-subtitle">Cada rincón fue pensado para que te sientas cómodo, presente y en conexión con la naturaleza.</p></div><div className="gallery-grid">{images.map((src,index) => <button className="gallery-item visible" key={src} onClick={() => setActive(index)} aria-label={`Abrir imagen ${index + 1}`}><Image src={src} alt={`Cielito Lindo - imagen ${index + 1}`} width={1400} height={1000}/><span className="gallery-item__overlay"><span className="gallery-item__expand">⊕</span></span></button>)}</div></div></section><div id="lightbox" className={active !== null ? 'active' : ''} onClick={() => setActive(null)}>{active !== null && <><button className="lb-btn" id="lb-close" onClick={() => setActive(null)}>×</button><button className="lb-btn" id="lb-prev" onClick={(event) => { event.stopPropagation(); next(-1); }}>←</button><Image id="lb-img" src={images[active]} alt="Imagen ampliada de Cielito Lindo" width={1600} height={1200} onClick={(event) => event.stopPropagation()}/><button className="lb-btn" id="lb-next" onClick={(event) => { event.stopPropagation(); next(1); }}>→</button><span id="lb-counter">{active + 1} / {images.length}</span></>}</div></>; }
